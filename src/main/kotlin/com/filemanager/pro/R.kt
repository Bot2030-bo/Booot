package com.filemanager.pro

object R {
    object layout {
        const val activity_main = android.R.layout.list_content
    }
    object id {
        const val recyclerView = android.R.id.list
        const val btnHome = android.R.id.home
    }
    object string {
        const val app_name = android.R.string.app_name
    }
    object style {
        const val Theme_FileManager = android.R.style.Theme_Light
    }
}